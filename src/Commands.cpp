#include "Commands.h"

// STUB: returns an empty string so the tests build and run.
// TODO: parse the command word, validate NAME (letters and spaces, in quotes)
// and ID (exactly 8 digits), call the matching AVLTree method, and format
// output: "successful"/"unsuccessful", comma-separated traversals
// ("Brian, Brandon, Briana"), and IDs printed as 8 digits (keep leading zeros).
std::string executeCommand(AVLTree& tree, const std::string& line) {
    (void)tree; (void)line;
    return "";
}
